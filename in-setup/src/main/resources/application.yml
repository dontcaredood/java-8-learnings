spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test-setup
    username: root
    password: Admin1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true

  application:
    name: SETUP

server:
  port: 8080