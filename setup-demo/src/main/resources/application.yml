server:
  port: 8888

spring:
  application:
    name: SETUP-DEMO
  datasource:
    url: jdbc:mysql://localhost:3306/test-setup?verifyServerCertificate=false&useSSL=true&requireSSL=true
    username: root
    password: Admin1234
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: true